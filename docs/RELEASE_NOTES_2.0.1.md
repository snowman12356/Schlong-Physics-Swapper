# Schlong Physics Swapper 2.0.1

- Fixed a crash before the main menu when SPS encountered certain filenames during its setup check.
- Improved handling of non-English filenames in setup checks and troubleshooting reports.

Update using the full package and keep your existing SPS INI settings. Update the DLL and included scripts together.

Physics behaviour and XML profiles are unchanged. The separate Predator head-collision test is not included.

The reported error was reproduced locally, and the fix passed regression tests and package validation. In-game confirmation from the affected user is still pending.
